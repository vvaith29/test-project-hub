package fundamentals.javaclasses;

public class IdentifiersAndReservedKeyWordsExamples {

	// Identifiers are basically it is name that we give to method,class,variables
	// and for labels

	// How an identifier should be?

	// An identifier should be have naming conventions as per (A-Z, a-z,0-9,$,_);

	// identifiers cannot have reserved keywords as their name ex(if,else,
	// for,switch);

	// But identifier can have predefined class name and interface name but it is
	// not recommended to use and we should not

	/*
	 * for example int $_= 100; (valid);
	 * 
	 * String total123="shiva"; (valid)
	 * 
	 * String 123total="eashwar"; (invalid)
	 * 
	 * int if=100; (invalid);
	 * 
	 * int else=200; (invalid);
	 * 
	 * int String=10; (valid)
	 * 
	 * int INTEGER=10;(valid)
	 * 
	 * int Runnable=400; (valid)
	 * 
	 * String @llHands="name" ;(invalid);
	 */

	// in java there are 53 reserved keyWords

	// in 53 reserved keywords the 3 keywords are reserved literals which is
	// true,false and null

	// true and false represents the boolean value and where as null represents the
	// default value for the object classes

	// for example Integer name = null ;

	/*
	 * Byte names = null;
	 * 
	 * String str = null;
	 * 
	 * Float floats = null;
	 * 
	 * System.out.println(names);
	 * 
	 * System.out.println(str1);
	 * 
	 * System.out.println(floats);
	 */

	public static void main(String[] args) {

	}

}
